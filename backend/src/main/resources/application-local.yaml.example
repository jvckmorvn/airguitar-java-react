spring:
  datasource:
    url: jdbc:mysql://localhost:3306/<DATABASE>?useSSL=false&serverTimezone=UTC
    username: <USER>
    password: <PASS>

  jpa:
    hibernate:
      ddl-auto: update

  server:
    port: 8080
